import type {AuditQuery} from '@/types';
import {Input} from '@/components/ui/Input';
import {Select,type Option} from '@/components/ui/Select';
import {Button} from '@/components/ui/Button';
export function AuditFilters({query,onChange,actors,actions}:{query:AuditQuery;onChange:(q:AuditQuery)=>void;actors:Option[];actions:Option[]}){const set=(key:keyof AuditQuery,value:string)=>onChange({...query,[key]:value});return <div className="filters"><Input label="Search audit history" placeholder="Actor, action or entity ID…" value={query.search||''} onChange={e=>set('search',e.target.value)}/><Input label="From date" type="date" value={query.from||''} onChange={e=>set('from',e.target.value)}/><Input label="To date" type="date" value={query.to||''} min={query.from} onChange={e=>set('to',e.target.value)}/><Select label="Actor" placeholder="All actors" options={actors} value={query.actorId||''} onChange={e=>set('actorId',e.target.value)}/><Select label="Action" placeholder="All actions" options={actions} value={query.action||''} onChange={e=>set('action',e.target.value)}/><Select label="Entity type" placeholder="All entities" options={['Employee','Asset','Organisation','Role','Identity'].map(v=>({value:v,label:v}))} value={query.entity||''} onChange={e=>set('entity',e.target.value)}/><Select label="Status" placeholder="All statuses" options={['Success','Failed','Pending'].map(v=>({value:v,label:v}))} value={query.status||''} onChange={e=>set('status',e.target.value)}/><div className="filter-actions"><Button variant="ghost" onClick={()=>onChange({})}>Clear filters</Button></div></div>;}

